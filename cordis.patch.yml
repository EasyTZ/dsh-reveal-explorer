# dsh-reveal-explorer 的 bundle 层：让 `dsh plugin add dsh-reveal-explorer`
# 装完就能用。没有它，dsh 的 plugin 命令只打印
#   warning: ... declares no dsh.bundle — installed as a plain dependency
# 然后把包装进去、永远不激活。

- insert:
    - id: dsdesktop-reveal-explorer
      name: '@easytz/dsh-reveal-explorer'
